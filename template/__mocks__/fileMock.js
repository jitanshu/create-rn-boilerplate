// SVG and image asset mock
const React = require('react');
module.exports = {
  default: (props) => React.createElement('svg', props),
  ReactComponent: (props) => React.createElement('svg', props),
};
